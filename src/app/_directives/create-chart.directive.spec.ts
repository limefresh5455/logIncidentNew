import { CreateChartDirective } from './create-chart.directive';

describe('CreateChartDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateChartDirective();
    expect(directive).toBeTruthy();
  });
});
