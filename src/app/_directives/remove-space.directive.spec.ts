import { RemoveSpaceDirective } from './remove-space.directive';

describe('RemoveSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
